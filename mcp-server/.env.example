# SpecifyPlus MCP Server Environment Variables

# Log level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Commands directory (defaults to .claude/commands/)
# COMMANDS_DIR=.claude/commands/

# Server name (displayed in MCP clients)
# SERVER_NAME=specifyplus-mcp-server
